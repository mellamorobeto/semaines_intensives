{% extends 'base.html.twig' %}

{% block title %}Website{% endblock %}
{% block stylesheets %}
  <link rel="stylesheet" href="style-companies.css">
  <link rel="stylesheet" href="./flexboxgrid/css/flexboxgrid.min.css" type="text/css" >
  {% endblock %}
{% block body %}
    <h1>Website</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ website.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ website.name }}</td>
            </tr>
            <tr>
                <th>Industrie</th>
                <td>{{ website.industrie }}</td>
            </tr>
            <tr>
                <th>Creer à</th>
                <td>{{ website.createdAt ? website.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Entreprise associé</th>
                <td>{{ website.company.type }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ website.statut }}</td>
            </tr>
        </tbody>
    </table>
    <body>



    <a href="{{ path('website_index') }}">back to list</a>

    <a href="{{ path('website_edit', {'id': website.id}) }}">edit</a>

    {{ include('website/_delete_form.html.twig') }}
{% endblock %}